version: "3.7"

services:
  cdk:
    build:
      context: ../
      args:
        - GITHUB_REGISTRY_TOKEN
    environment:
      - ENVIRONMENT
      - STEP_ENVIRONMENT
      - VERSION
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY
      - AWS_SESSION_TOKEN
      - AWS_REGION
      - AWS_DEFAULT_REGION
      - GCP_REGION
      # provided by the agent
      - NODE_OPTIONS
